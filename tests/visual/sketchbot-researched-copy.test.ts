import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Sketchbot guards throughput, modes and photo handoff',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-sketchbot.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[2].answer,/отдельной проверки/);assert.match(d.blocks.faq[6].answer,/подтверждают отдельно/);assert.doesNotMatch(JSON.stringify(d.blocks),/45 секунд|30 гостей в час|5 роботов|интернет не нужен/);});
test('Sketchbot shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-sketchbot': sketchbotRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-sketchbot"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
