import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('KettyBot guards dock and dialogue',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-kettybot.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.seoIntent.entity.model,'KettyBot');assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[5].answer,/поставляется отдельно/);assert.match(d.blocks.faq[2].answer,/не равны свободному/);});
test('KettyBot shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-kettybot': kettybotRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-kettybot"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
