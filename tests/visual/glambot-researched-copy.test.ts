import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('GlamBot separates capture, processing and delivery',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-glambot.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[3].answer,/разные этапы/);assert.match(d.blocks.faq[6].answer,/не означает автоматическую публикацию/);assert.doesNotMatch(JSON.stringify(d.blocks),/240 FPS|20 секунд|40 гостей|вирусность гарантирована/);});
test('GlamBot shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-glambot': glambotRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-glambot"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
