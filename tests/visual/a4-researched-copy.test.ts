import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('A4 guards separate format, speed and photo handling',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-robota-hudozhnika-a4.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[0].answer,/отдельные портретные форматы/);assert.match(d.blocks.faq[6].answer,/не означает разрешения/);assert.doesNotMatch(JSON.stringify(d.blocks),/45 секунд|за 5 минут|xArm 6|большого формата/);});
test('A4 shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-robota-hudozhnika-a4': a4Remediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-robota-hudozhnika-a4"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
