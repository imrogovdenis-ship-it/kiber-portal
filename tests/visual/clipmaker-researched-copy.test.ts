import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Clipmaker distinguishes video service and guarded installation',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-klipmeiker.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[0].answer,/Репортажная съёмка/);assert.match(d.blocks.faq[3].answer,/характеристикам опоры/);assert.doesNotMatch(JSON.stringify(d.blocks),/12,6 кг|6 степеней свободы|4 кг|абсолютно безопасен/);});
test('Clipmaker shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-klipmeiker': clipmakerRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-klipmeiker"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
