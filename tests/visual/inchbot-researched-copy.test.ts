import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Inchbot EDU copy guards exact configuration',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-inchbot-l1-w-edu.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.seoIntent.entity.model,'L1-W EDU');assert.equal(d.seo.primaryKeyword,d.seoIntent.primaryKeyword);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.aiSummary,/само по себе не подтверждает/);assert.match(d.blocks.faq[1].answer,/нельзя автоматически/);assert.doesNotMatch(JSON.stringify(d.blocks),/IP54|Jetson|12 кг|5,5 м|VR|в современном мире/);});
test('Inchbot uses shared mapping and sizing',()=>{const d=readFileSync('src/lib/kiber94-robot-template-data.ts','utf8');assert.match(d,/'arenda-inchbot-l1-w-edu': inchbotRemediation/);const t=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(t,/'arenda-inchbot-l1-w-edu': \['arenda-xiaomi-cyberdog-2', 'arenda-unitree-go2', 'arenda-promobot-v4', 'arenda-kettybot'\]/);assert.match(t,/style=\{\[[^\]]*"arenda-inchbot-l1-w-edu"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
