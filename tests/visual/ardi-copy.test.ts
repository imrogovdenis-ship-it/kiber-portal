import assert from'node:assert/strict';import{existsSync,readFileSync}from'node:fs';import{test}from'node:test';test('Ardi copy distinguishes expressive interaction from walking humanoids',()=>{const f='data/content/robot-card-pilot/arenda-robota-ardi.json';assert(existsSync(f),'Ardi package missing');const d=JSON.parse(readFileSync(f,'utf8'));assert.equal(d.seo.primaryKeyword,'аренда робота Арди');assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(new Set(d.blocks.faq.map((x:any)=>x.question)).size,8);assert(d.blocks.faq.every((x:any)=>x.answer.length>100));assert(!/\b(13|15|600)\b/.test(JSON.stringify(d.blocks)),'do not universalize disputed figures');assert.equal(d.review.publicationApproved,false);assert(!d.related.robotSlugs.includes('arenda-robota-ardi'));});