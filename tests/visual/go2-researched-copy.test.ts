import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Go2 has complete guarded exact-model copy',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-unitree-go2.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.seoIntent.entity.model,'Go2');assert.equal(d.seo.primaryKeyword,d.seoIntent.primaryKeyword);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.aiSummary,/Air, Pro и EDU/);assert.match(d.blocks.faq[1].answer,/не обещается/);assert.doesNotMatch(JSON.stringify(d.blocks),/12 км|1,5 часа|в современном мире|уникальное решение/);});
test('Go2 uses shared model copy and sizing',()=>{const d=readFileSync('src/lib/kiber94-robot-template-data.ts','utf8');assert.match(d,/'arenda-unitree-go2': go2Remediation/);const t=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(t,/'arenda-unitree-go2': go2Editorial.related.robotSlugs/);assert.match(t,/style=\{\[[^\]]*"arenda-unitree-go2"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
