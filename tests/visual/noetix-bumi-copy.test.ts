import assert from'node:assert/strict';import{existsSync,readFileSync}from'node:fs';import{test}from'node:test';test('Noetix Bumi has a guarded model-specific rental package',()=>{const f='data/content/robot-card-pilot/arenda-noetix-bumi.json';assert(existsSync(f),'Bumi copy missing');const d=JSON.parse(readFileSync(f,'utf8'));assert.equal(d.seo.primaryKeyword,'аренда Noetix Bumi');assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(new Set(d.blocks.faq.map((x:any)=>x.question)).size,8);assert(d.blocks.faq.every((x:any)=>x.answer.length>100));const t=JSON.stringify(d.blocks);assert(!/\b(94|98|12|17|21)\b/.test(t),'disputed edition figures must not become universal');assert(t.includes('EDU Pro'));assert.equal(d.review.publicationApproved,false);assert(!d.related.robotSlugs.includes('arenda-noetix-bumi'));});