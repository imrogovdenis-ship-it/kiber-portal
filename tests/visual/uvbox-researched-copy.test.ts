import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('UV-BOX separates screen variants and optional data integrations',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-uv-box.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[1].answer,/нужно различать/);assert.match(d.blocks.faq[4].answer,/не следует считать включёнными автоматически/);assert.doesNotMatch(JSON.stringify(d.blocks),/85%|60%|40%|32 дюйма/);});
test('UV-BOX shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-uv-box': uvboxRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-uv-box"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});

test('UV-BOX CTA is a display, not a robot',()=>{assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/Арендуйте интерактивную витрину UV-BOX для мероприятия/);});
