import assert from'node:assert/strict';import{existsSync,readFileSync}from'node:fs';import{test}from'node:test';
test('Unitree H2 has researched model-specific copy with configuration guards',()=>{const f='data/content/robot-card-pilot/arenda-unitree-h2.json';assert(existsSync(f),'H2 copy package missing');const d=JSON.parse(readFileSync(f,'utf8'));assert.equal(d.seo.primaryKeyword,'аренда Unitree H2');assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(new Set(d.blocks.faq.map((x:any)=>x.question)).size,8);assert(d.blocks.faq.every((x:any)=>x.answer.length>100));const t=JSON.stringify(d.blocks);assert(t.includes('Unitree H2 EDU'));assert(!t.includes('Unitree h2'));assert(!t.includes('абсолютно безопасен'));assert.equal(d.review.publicationApproved,false);assert(!d.related.robotSlugs.includes('arenda-unitree-h2'));});
