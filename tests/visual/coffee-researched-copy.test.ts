import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Coffee copy separates station, utilities and servicing',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-robo-kofeyni.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[3].answer,/сбор использованной воды/);assert.match(d.blocks.faq[7].answer,/очистка/);assert.doesNotMatch(JSON.stringify(d.blocks),/450 напитков|за 90 секунд|24\/7|не требует воды/);});
test('Coffee shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-robo-kofeyni': coffeeRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-robo-kofeyni"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
