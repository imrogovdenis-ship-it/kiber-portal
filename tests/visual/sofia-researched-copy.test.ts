import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Sophia copy uses confirmed identity and six/eight guarded blocks',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-robota-sofiya.json','utf8'));assert.equal(d.seoIntent.entity.manufacturer,'Hanson Robotics');assert.equal(d.seoIntent.entity.model,'Sophia');assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.equal(d.seo.canonicalPath,'/robots/arenda-robota-sofiya/');assert.equal(d.seo.primaryKeyword,d.seoIntent.primaryKeyword);assert.ok(d.blocks.goshaQuote.includes('\n\n'));const text=JSON.stringify(d.blocks);assert.doesNotMatch(text,/60\+|167 см|83 степени|в современном мире|уникальное решение/i);assert.match(text,/Little Sophia/);assert.match(text,/не означает/);for(const arr of [d.blocks.capabilities,d.blocks.scenarios])assert.equal(new Set(arr.map((x: {title: string})=>x.title)).size,6);});
test('Sophia is wired to model-scoped pilot and inherited geometry',()=>{const data=readFileSync('src/lib/kiber94-robot-template-data.ts','utf8');assert.match(data,/'arenda-robota-sofiya': sofiaRemediation/);const template=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(template,/'arenda-robota-sofiya': sofiaEditorial.related.robotSlugs/);assert.match(template,/style=\{\[[^\]]*"arenda-robota-sofiya"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
