import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('BellaBot copy separates delivery and handling',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-bellabot.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.seoIntent.entity.model,'BellaBot');assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.aiSummary,/не перекладывает/);assert.match(d.blocks.faq[1].answer,/разные модели/);assert.doesNotMatch(JSON.stringify(d.blocks),/безупречный|24 часа|без ожидания|в современном мире/);});
test('BellaBot uses shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-bellabot': bellabotRemediation/);const t=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(t,/'arenda-bellabot': bellabotEditorial.related.robotSlugs/);assert.match(t,/style=\{\[[^\]]*"arenda-bellabot"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
