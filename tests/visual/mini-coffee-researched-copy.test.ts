import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Mini Coffee guards module identity and utilities',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-mini-robo-kofeyni.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[1].answer,/не следуют из названия/);assert.match(d.blocks.faq[2].answer,/не означает настольное/);assert.doesNotMatch(JSON.stringify(d.blocks),/4 кВт|7 видов кофе|3 вида мороженого|не требует воды/);});
test('Mini Coffee shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-mini-robo-kofeyni': miniCoffeeRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-mini-robo-kofeyni"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
