import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('CyberDog 2 has complete exact-generation copy',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-xiaomi-cyberdog-2.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.seoIntent.entity.model,'CyberDog 2');assert.equal(d.seo.primaryKeyword,d.seoIntent.primaryKeyword);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.aiSummary,/второго поколения/);assert.match(d.blocks.faq[3].answer,/не обещается/);assert.doesNotMatch(JSON.stringify(d.blocks),/13 Мп|15 км|8,9 кг|в современном мире|уникальное решение/);});
test('CyberDog uses shared mapping and sizing',()=>{const d=readFileSync('src/lib/kiber94-robot-template-data.ts','utf8');assert.match(d,/'arenda-xiaomi-cyberdog-2': cyberdogRemediation/);const t=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(t,/'arenda-xiaomi-cyberdog-2': cyberdogEditorial.related.robotSlugs/);assert.match(t,/style=\{\[[^\]]*"arenda-xiaomi-cyberdog-2"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
