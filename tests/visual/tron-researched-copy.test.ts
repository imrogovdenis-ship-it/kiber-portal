import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Tron has complete guarded model copy',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-robota-tron.json','utf8'));assert.equal(d.slug,'arenda-robota-tron');assert.equal(d.seoIntent.entity.model,'Tron');assert.equal(d.seoIntent.entity.manufacturer,undefined);assert.equal(d.review.publicationApproved,false);assert.equal(d.seo.primaryKeyword,d.seoIntent.primaryKeyword);assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.doesNotMatch(JSON.stringify(d.blocks),/1000|12 степен|в современном мире|уникальное решение|TRON2|TRON 2/);for(const a of [d.blocks.capabilities,d.blocks.scenarios])assert.equal(new Set(a.map((x:{title:string})=>x.title)).size,6);});
test('Tron copy and image sizing use the existing shared template',()=>{const data=readFileSync('src/lib/kiber94-robot-template-data.ts','utf8');const t=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(data,/'arenda-robota-tron': tronRemediation/);assert.match(t,/tronEditorial.related.robotSlugs/);assert.match(t,/style=\{\[[^\]]*"arenda-robota-tron"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
