import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Promobot has complete model-specific guarded copy',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-promobot-v4.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.seoIntent.entity.manufacturer,'Promobot');assert.equal(d.seo.primaryKeyword,d.seoIntent.primaryKeyword);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.capabilities[5].title,/при наличии/);assert.match(d.blocks.capabilities[2].text,/данных/);assert.doesNotMatch(JSON.stringify(d.blocks),/8 часов|семи языках|100 тысяч|в современном мире|уникальное решение/i);});
test('Promobot inherits shared wiring and actual sizing',()=>{const d=readFileSync('src/lib/kiber94-robot-template-data.ts','utf8');assert.match(d,/'arenda-promobot-v4': promobotRemediation/);const t=readFileSync('src/components/templates/RobotCardTemplate.astro','utf8');assert.match(t,/'arenda-promobot-v4': promobotEditorial.related.robotSlugs/);assert.match(t,/style=\{\[[^\]]*"arenda-promobot-v4"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
