import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
test('Robobar has guarded menu and service copy',()=>{const d=JSON.parse(readFileSync('data/content/robot-card-pilot/arenda-robot-barmen.json','utf8'));assert.equal(d.blocks.capabilities.length,6);assert.equal(d.blocks.scenarios.length,6);assert.equal(d.blocks.faq.length,8);assert.equal(d.review.publicationApproved,false);assert.ok(d.blocks.goshaQuote.includes('\n\n'));assert.match(d.blocks.faq[1].answer,/Нет, меню зависит/);assert.match(d.blocks.faq[6].answer,/не заменяет всю команду/);assert.doesNotMatch(JSON.stringify(d.blocks),/за 60 секунд|за одну минуту|20 коктейлей/);});
test('Robobar shared mapping and sizing',()=>{assert.match(readFileSync('src/lib/kiber94-robot-template-data.ts','utf8'),/'arenda-robot-barmen': robobarRemediation/);assert.match(readFileSync('src/components/templates/RobotCardTemplate.astro','utf8'),/style=\{\[[^\]]*"arenda-robot-barmen"[^\]]*\]\.includes\(robotSlug\) \? "height:auto;object-position:center"/);});
