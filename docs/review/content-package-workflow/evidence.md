# KIBER content package workflow evidence

## Scope

- Branch: `hermes/content-package-workflow-20260828`
- Base: `codex/kiber-15-controlled-rebuild` at `22ce2a6a7f19b196304d6166e09259e531eb37ac`
- Safe autonomous task selected while Александр is offline: content package schema/workflow.
- Production-impacting actions excluded: no deploy, no DNS changes, no production secrets, no analytics provider IDs/cookies, no live lead routing, no real public contacts.

## What this adds

- `data/review/content-package-workflow.json` records the review-only content package workflow across:
  - structural content acceptance;
  - media rights registry;
  - lead capability-only contract;
  - legal documents sourced from live kiber-portal.ru per owner decision.
- `scripts/content-package-workflow-smoke.mjs` verifies the workflow remains human-gated and destination-free.
- `tests/visual/content-package-workflow-contract.test.ts` locks the contract and CI script wiring.
- `docs/review/content-package-workflow/report.json` is generated by the smoke gate.

## TDD evidence

RED:

```text
node --import tsx --test tests/visual/content-package-workflow-contract.test.ts
# not ok 1 - content package workflow contract is required
# not ok 2 - content package workflow smoke is required
```

GREEN:

```text
node --import tsx --test tests/visual/content-package-workflow-contract.test.ts && npm run test:content-package-workflow
# pass 2
# KIBER content package workflow smoke passed: 4 sections ready for human review; production publish remains gated.
```

## Latest smoke report

```json
{
  "issue": "KIBER-content-package-workflow",
  "packageSections": 4,
  "robotPages": 24,
  "launchPages": 7,
  "legalDocuments": 3,
  "finalApproved": 0,
  "leadRoutingEnabled": false,
  "productionApprovedMedia": 0,
  "status": "passed",
  "failures": [],
  "warnings": []
}
```

## Remaining blockers

This pass intentionally does not approve or publish the content package. Human approval is still required for final business/legal/media acceptance, real public contacts, live lead destinations, and any production deployment.
