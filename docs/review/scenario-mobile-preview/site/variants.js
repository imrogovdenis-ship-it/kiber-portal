(()=>{const s=document.querySelector('.humanoid-template__scenario-strip');if(!s)return;const cards=[...s.children];const buttons=[...document.querySelectorAll('[data-proposal-slide]')];buttons.forEach((b,i)=>b.addEventListener('click',()=>s.scrollTo({left:cards[i].offsetLeft-cards[0].offsetLeft,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})));s.addEventListener('scroll',()=>{let i=cards.reduce((best,c,n)=>Math.abs(c.offsetLeft-cards[0].offsetLeft-s.scrollLeft)<Math.abs(cards[best].offsetLeft-cards[0].offsetLeft-s.scrollLeft)?n:best,0);buttons.forEach((b,n)=>b.setAttribute('aria-current',String(n===i)))},{passive:true});})();