# Wordstat remote browser access — prepared

The dedicated Wordstat access path is now verified:

```text
Alexander Windows Chrome dedicated profile
→ Chrome remote debugging on 127.0.0.1:9222
→ SSH reverse tunnel
→ Hermes CDP/Playwright connection
→ Yandex Wordstat live table extraction
```

See `RUNBOOK.md` for the exact operating procedure and safety boundaries.

Verified query: `аренда робота`, total `2 161`, period `06.08.2026 – 04.09.2026`.
