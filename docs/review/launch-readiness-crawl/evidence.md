# KIBER launch readiness critical route evidence

## Scope

Safe autonomous cron slice while Александр is offline/in-flight: tighten the controlled rebuild launch-readiness crawl so critical commercial/contact/lead/legal routes are explicitly listed and checked against the launch route registry.

This is preview/repo-only evidence. No production deploy, DNS, production secrets, analytics provider IDs/cookies, or live lead routing were touched.

## Changes

- `data/review/launch-readiness-crawl.json` now records `criticalRoutes` for:
  - `/` commercial entry
  - `/contacts/` placeholder contact trust page
  - `/lead/request/` capability-only lead entry, not in sitemap
  - `/lead/thanks/` non-indexed confirmation, not in sitemap
  - `/privacy-policy/`, `/consent/`, `/cookie-policy/` live-site sourced legal pages, not in sitemap
- `tests/visual/launch-readiness-crawl.test.ts` covers the new critical route contract and confirms the smoke gate validates it.
- `scripts/launch-readiness-crawl-smoke.mjs` verifies each critical route exists in `data/seo/launch-routes.json` and its sitemap flag matches the readiness contract.
- `docs/review/launch-readiness-crawl/report.json` was regenerated by the smoke gate after `npm run ci`.

## TDD / validation

RED:

```text
node --import tsx --test tests/visual/launch-readiness-crawl.test.ts
# fail: registry.criticalRoutes was undefined and smoke script did not reference criticalRoutes
```

GREEN / full verification:

```text
node --import tsx --test tests/visual/launch-readiness-crawl.test.ts
# tests 2, pass 2, fail 0

npm run ci
# KIBER launch readiness crawl passed: 36 routes, 24 robots; go/no-go remains blocked by 6 owner decisions.
# KIBER-20 CI baseline smoke passed: 37 HTML pages link-checked, dist/404.html verified, 1475 tracked files secret-scanned.
```

## Remaining blockers

Launch go/no-go remains blocked until explicit owner decisions for:

- final visual approval for pending visual PRs;
- real public contact channels;
- live lead routing destinations and backend;
- media rights/legal approval for production assets;
- analytics provider decision and IDs;
- explicit production deploy permission.
