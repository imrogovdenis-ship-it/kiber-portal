# Content package — Unitree G1 на корпоративе

Status: draft_for_owner_review  
Page type: article_detail  
Preview route: `/preview/kiber-94/articles/unitree-g1-na-korporative/`

## Source documents

- `source/original-draft.docx`
- `source/original-draft.md`
- `source/hero-upload.png`

## Research

### Wordstat

Live Wordstat checked in visible Chrome/CDP. Direct corporate queries are low/zero, while exact model queries are strong and generic event queries support context.

See `wordstat-summary.md` and `data/seo/wordstat-research/article-unitree-g1-korporativ-20260908/*.wordstat.json`.

### SERP

- `Unitree G1 на корпоратив`: 10 results captured; SERP is mixed between ads, robot rental pages, model pages and generic event-service results.
- `робот Unitree G1 на корпоратив`: 10 results captured; SERP is mixed between ads, robot rental pages, model pages and generic event-service results.
- `аренда Unitree G1`: 10 results captured; SERP is mixed between ads, robot rental pages, model pages and generic event-service results.
- `робот на корпоратив`: 10 results captured; SERP is mixed between ads, robot rental pages, model pages and generic event-service results.
- `аренда роботов на мероприятие`: 10 results captured; SERP is mixed between ads, robot rental pages, model pages and generic event-service results.

## Content gaps / angle

The useful angle is not another generic rental landing and not a model specification card. The article explains how Unitree G1 can be distributed across a corporate event: welcome, stage transition, networking/photo point, closing photo, and operator/prep requirements.

## SEO decision

- Primary: `Unitree G1 на корпоративе` — exact model + scenario article intent.
- `Unitree G1 аренда` / `аренда Unitree G1` support links to the robot card.
- `аренда роботов на мероприятие` and `робот для мероприятия` are bridge/context terms, not primary.

## Media

- Hero: `/images/articles/unitree-g1-korporativ/hero.webp`
- Additional gallery: reused from Unitree G1 robot-card gallery, as allowed by Alexander.

## Pricing note

The DOCX contains an operational tariff table, but the rendered article uses current structured robot-card price wording and request-to-manager language to avoid contradicting approved site data.

## Review boundaries

Owner review required. This package does not approve merge, production, DNS, secrets, analytics, live lead routing, public route replacement, mass generation, or `/articles/` assembly.
