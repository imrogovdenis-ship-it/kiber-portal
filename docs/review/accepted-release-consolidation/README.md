# Единый принятый кандидат КИБЕР ПОРТАЛА

**Production: NO_GO до отдельных разрешений.**

Предпросмотр: https://jino-preview.kiber-portal.ru/

## Состав
Ветка наследует принятую цепочку карточек до PR120 (все 24) и добавляет отсутствовавшие изменения PR95 (два навигационных блока шести статей) и PR94 (исходники подготовленного PHP/API-моста, без изменения его runtime). Не требуется слепо мержить все открытые PR: `open-pr-map.json` — снимок; старые ветки могут быть уже содержательно включены в main. PR консолидации нацелен на PR120, чтобы показывать только добавку. Перед разрешённым merge сверить ancestry/patch equivalence, затем выполнять цепочку снизу вверх; не закрывать и не перезаписывать PR автоматически.

## Исправления CI
Удалены девять затенённых ключей relatedSlugsByRobotSlug с сохранением последних, реально отображавшихся значений. Добавлен тест уникальности. Старое ожидание A4 приведено к явному решению владельца. Тест CTA проверяет фактическое вычисление заголовка, а не устаревшую строку исходника. Inchbot проверяет фактически принятый состав related cards, а не затенённую декларацию.

## Проверки и границы
`parity.json`: 43 страницы; медиа/ссылки сохранены, 25 CSS/JS совпадают с размещёнными побайтно. Единственные изменения видимого текста сверх восстановления принятого PR95 — ранее одобренное имя художника без A4 и устранение «робота робота» в CTA. `deployed.json`: SHA256 каждой реально скачанной после обновления HTML-страницы.

Проверки локальных контрактов/бюджетов/мобильной геометрии прошли. Один полный локальный запуск превысил лимит инструмента: его exit PASS не заявляется. Авторитетный итог CI — checks точного HEAD этого PR. Статус/API JSON POST/HTML redirect действующего Jino preview прошли в dry-run; каналы пропущены, это не новая live-доставка.

## Изменённое окружение
Только 43 HTML-файла в `/domains/jino-preview.kiber-portal.ru/`, без замены assets, .htaccess, bridge.php, private config, API image, DNS или production. Noindex и предупреждение dry-run сохранены.

## Откат
Предыдущие HTML и SFTP-команды хранятся в `/home/alex/.hermes/state/kiber-release-consolidation/approved-baseline` и `rollback.sftp`. Выполнить существующим `jino-sftp-run.py` только после решения об откате, затем скачать все пути и сверить с snapshot; проверить noindex и GET /api/leads/status=dry-run. Не откатывать чужие файлы или серверные настройки. Snapshot не содержит секретов и не хранится в публичном document root.

## До публикации
1. Зелёный hosted CI точного HEAD и разрешение merge.
2. После merge — rebuild из main и проверка exact-SHA кандидата.
3. Отдельное разрешение публикации, DNS (если нужны), режима live и production-конфигурации; сохранение прежних DNS/образов для отката.
4. Контроль HTTPS, индексации, маршрутов, безопасности и форм без повторной реальной заявки без разрешения.
Аналитику не активировать. Ранее принятую live-доставку не повторять автоматически. Предпросмотр не равен публикации.
