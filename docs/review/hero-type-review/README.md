# Hero and typography: owner comparison R1

Preview-only disposable design prototypes; **not production Astro components**. Owner approval pending. Homepage/runtime source untouched. No merge or production publication authorized.

## Review
- https://jino-preview.kiber-portal.ru/preview/hero-type-review/hero.html
- https://jino-preview.kiber-portal.ru/preview/hero-type-review/typography.html

Hero: A full-width cover, B enlarged side image, C full-width title + image/description spread. Same real H1/intro/images for collection/article; source image 1600×900. Natural aspect ratio, contain, no crop. Existing buttons omitted only from this isolated three-element comparison, not from full pages or production.

Typography A/B: full frozen article, dog compilation, article index, compilation index. Existing colors/copy/block composition retained; shared type CSS has no production import. H1/H2/H3 roles unified; small card copy retains a subordinate size. Hero composition stays original on these pages so the two decisions are independent. The half-width sample cards on the overview are compact specimens; the listed pixel scales apply to linked full-page examples.

## Evidence
- local 40 and deployed 40 route/viewports PASS: 390/768/1280/1440.
- images decoded, no horizontal overflow, identical heading sizes per role, six uncropped Hero ratios, noindex, no tracker/API requests.
- all eight full-page main text sequences exactly equal frozen originals.
- 33 deployed file hashes matched; seven protected production/preview pages byte-identical.
- 29 existing same-origin preview images reused only after production/preview SHA256 equality. CSP not changed.
- deterministic generator regenerated byte-identical outputs. No npm install or full repository duplication.

## Reproduce
`python3 generate.py` (BeautifulSoup4 required; locally available from the existing vendor environment). Baselines and two images are included. Existing safe UI scripts and handcrafted CSS remain in site/. Fonts/CSS cache in site/assets. Generator caches source styles/assets. media-map.json fixes source image references to verified same-origin preview assets. To reproduce independently, retain those matching shared assets or provision them under the preview root. Do not loosen CSP to allow external production images.

`node qa.mjs <absolute-site-directory|live> <output-directory>` uses the existing Playwright installation noted in the import; adapt the import for a different environment. Local interception does not itself reproduce Apache CSP; deployed checks are mandatory.

## Scope / rollback
Only /preview/hero-type-review/ was added. Remove exactly the paths in release-manifest.json, then its assets directory and root directory. Existing root assets, API, hosting configuration, prior previews and main site must remain untouched. Private SFTP scripts and screenshots are held in the session state, not this package.

## Next approval
Select Hero A/B/C and typography A/B independently. Then author one component-level correction/specification sheet and a shared Astro implementation, preserving the homepage. No full-site propagation before that choice.
