<section id="cookie-consent" class="cookie-consent" role="region" aria-labelledby="cookie-consent-title" hidden>
  <div class="cookie-consent__inner">
    <div class="cookie-consent__copy">
      <p id="cookie-consent-title" class="cookie-consent__title">Настройки cookie</p>
      <p>Мы используем cookie и похожие технологии для сохранения ваших настроек. Необязательные cookie для статистики и рекламы — только с вашего согласия. <a href="/cookie-policy/">Политика cookie</a></p>
    </div>
    <div class="cookie-consent__actions">
      <button type="button" data-cookie-choice="rejected">Отклонить</button>
      <button type="button" data-cookie-choice="accepted">Принять</button>
    </div>
  </div>
</section>
<script is:inline src="/scripts/cookie-consent.js?v=2" defer></script>
<style>
  .cookie-consent[hidden] { display: none; }
  .cookie-consent { position: fixed; inset: auto 0 0; z-index: 90; background: var(--kp-white); color: var(--kp-ink-dark); border-top: 0.0625rem solid var(--kp-line); box-shadow: 0 -0.25rem 1.5rem color-mix(in srgb, var(--kp-ink-dark) 8%, transparent); max-height: 60dvh; overflow: auto; padding-bottom: env(safe-area-inset-bottom); }
  .cookie-consent__inner { max-width: 80rem; margin: auto; padding: 1.25rem 1.5rem; display: flex; align-items: center; gap: 1.75rem; }
  .cookie-consent__copy { flex: 1; min-width: 0; }
  .cookie-consent p { margin: 0; font: 400 0.875rem/1.55 var(--kp-font-body, Montserrat, Arial, sans-serif); }
  .cookie-consent .cookie-consent__title { margin-bottom: 0.375rem; font-weight: 700; font-size: 1rem; }
  .cookie-consent a { color: var(--kp-blue-deep); text-decoration: underline; text-underline-offset: 0.1875rem; }
  .cookie-consent__actions { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0.75rem; flex-shrink: 0; }
  .cookie-consent button { min-height: 2.75rem; padding: 0.625rem 1.125rem; background: white; border: 0.0625rem solid var(--kp-blue-deep); border-radius: 0.625rem; color: var(--kp-blue-deep); font: 600 0.875rem/1.4 var(--kp-font-body, Montserrat, Arial, sans-serif); cursor: pointer; }
  .cookie-consent button:hover { background: var(--kp-sky); }
  .cookie-consent :focus-visible { outline: 0.1875rem solid var(--kp-blue-deep); outline-offset: 0.1875rem; }
  :global([data-cookie-settings]) { background: none; border: 0; padding: 0; color: inherit; font: inherit; cursor: pointer; text-align: left; text-decoration: underline; text-underline-offset: 0.1875rem; }
  :global(body:has(#cookie-consent:not([hidden]))) { padding-bottom: var(--kp-cookie-banner-height, 15rem); }
  @media (max-width: 47.9375rem) { .cookie-consent__inner { padding: 1rem; flex-direction: column; align-items: stretch; gap: 0.875rem; } .cookie-consent__actions { width: 100%; } }
</style>
