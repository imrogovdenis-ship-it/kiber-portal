# Generated from data/seo/redirects.json for KIBER-29.
# Keep entries exact-match only; wildcard/site-wide redirects require an explicit review.

location = /test-blok/ {
  return 301 /;
}

location = /test-blok {
  return 301 /;
}
