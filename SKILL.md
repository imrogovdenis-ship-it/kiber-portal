---
name: kiber-portal-brand
description: Фирменный стиль «Кибер Портал» — сайт и любые визуальные материалы по аренде и продаже роботов. Применяй при создании ЛЮБОГО фронтенд-артефакта для Кибер Портала: лендинг, страница робота, презентация, письмо, баннер, дашборд, HTML/CSS/React. Триггерь также на «Кибер Портал», «Кибер Гоша», «сайт по роботам», «страница аренды робота», «в стиле Кибер Портала», а также на упоминания синего #0088FF, шрифта Gilroy или Tilda-сайта по робототехнике. Используй ДО написания первой строки CSS.
---

# Кибер Портал — Design System

Бренд по аренде и продаже сервисных роботов: гуманоиды, робо-собаки, официанты, промо-роботы.
Тон визуала — **технологичный, но не холодный**: много белого воздуха, один насыщенный синий на действие, тёмные фотообложки с живыми роботами.

## Правила, которые нельзя нарушать

1. **Синий = действие.** `#0088FF` появляется только на кнопках, ссылках, иконках, цифрах-статистике и логотипе. Если синий элемент не кликабельный и не число — это ошибка.
2. **Один шрифт.** Gilroy, три веса: 500 текст, 600 интерфейс, 700 заголовки. Montserrat — только legacy-блоки Tilda, в новом коде не появляется.
3. **Скругление 18px** — подпись бренда. Все карточки, фото и медиа-блоки. Кнопка 24px, инпут 5px, тег 50px.
4. **Теней нет.** Глубину даёт заливка `#F4F8FF` или граница `#E7E7E7`.
5. **Одна primary-кнопка на экран.** Вторая — secondary (белая, синий текст) или ghost (только на тёмной обложке).
6. **Тёмное — через `.kp-on-dark`**, а не ручной перекраской текста.
7. **Заголовки с line-height 1.0** рассчитаны на 2–3 слова в строке. Длинный H1 режем по смыслу, а не жмём интерлиньяж.

## Токены

Всё в `colors_and_type.css`. Никогда не хардкодь hex — бери переменную.

| Роль | Токен | Значение |
|---|---|---|
| Действие | `--kp-blue` | `#0088FF` |
| Hover / pressed | `--kp-blue-deep` | `#005EFF` |
| Поверхность-подложка | `--kp-blue-tint` | `#F4F8FF` |
| Тёмная секция, обложка | `--kp-ink-900` | `#25222B` |
| Заголовки на светлом | `--kp-ink-800` | `#36323E` |
| Основной текст | `--kp-gray` | `#797A91` |
| Подписи, текст на тёмном | `--kp-gray-light` | `#A1A2B8` |
| Границы | `--kp-border` | `#E7E7E7` |
| Граница инпута | `--kp-border-input` | `#C9C9C9` |
| Акцент (срочность, рейтинг) | `--kp-amber` | `#FF991D` |

**Типографика.** H1 64/1.0/700 · H2 44/1.0/700 · Stat 34/1.0/700 синим · H3 22/1.4/700 · Lead 20/1.55/500 · Body 16/1.6/500 · Caption 14/1.2/600.
Мобильные: H1 → 32, H2 → 26, body → 15.

**Ритм.** База 5px. Секция — 75px сверху/снизу (60 планшет, 45 мобайл), блоки внутри — 30px. Контейнер 1200px, gutter 20px, 12 колонок. Брейкпоинты 1200 / 960 / 640 / 480.

**Пропорция цвета на странице:** ~60% белого, ~22% тинта `#F4F8FF`, ~10% тёмного `#25222B`, ~8% синего. Amber — максимум один элемент на экран.

## Компоненты

| Компонент | Файл | Когда |
|---|---|---|
| `Nav` | `components/Nav/` | Белая фиксированная шапка 80px (64 мобайл), лого 100px, одна primary-кнопка |
| `Hero` | `components/Hero/` | Фотообложка 50vh + скрим `rgba(37,34,43,.20)→.80`, единственное место для H1 |
| `ProductCard` / `ProductGrid` | `components/ProductCard/` | Каталог роботов: фото 4:3, тег, название, цена синим |
| `FeatureGrid` | `components/FeatureGrid/` | «Что входит» / «Почему мы» — иконка в белом круге + строка статистики |
| `ExpertQuote` | `components/ExpertQuote/` | «Кибер Гоша» — рекомендация от первого лица на тинте, обычно перед ценой |
| `Faq` | `components/Faq/` | Аккордеон с ценой первым вопросом, последний блок перед подвалом |
| `LeadForm` | `components/LeadForm/` | Заявка. **Максимум 4 поля** — больше живой сайт не спрашивает |
| `Footer` | `components/Footer/` | Тёмный подвал, лого в выворотке, ссылки `--kp-gray-light` |

Каркас страницы робота: шапка → обложка → вводный блок → «что умеет» → «где вау-эффект» → цитата Гоши → FAQ с ценой → похожие роботы → подвал.

Кнопки, инпуты, теги, карточки — классами из `components.css`: `.kp-btn--primary/--secondary/--ghost` × `--xs/--sm/--smd/--md/--lg`, `.kp-card--outline/--tint/--dark`, `.kp-tag--solid/--amber`, `.kp-input`.

## Как подключать

```html
<link rel="stylesheet" href="styles.css">
```

Один импорт тянет токены и компонентный слой. Для React-компонентов достаточно того же CSS — они опираются на CSS-переменные, а не на CSS-in-JS тему.

## Контент

Заголовки — по формуле «действие + объект + география»: «Аренда робота-официанта в Москве». Описания короткие, с конкретикой (что входит, за сколько, как быстро). Цену показываем всегда, даже «от». Гости, мероприятие, вау-эффект — это язык клиента; «квадропед», «манипулятор» — только в техблоке.
